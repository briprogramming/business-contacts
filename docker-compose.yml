services:
  app:
    build:
      context: .
      dockerfile: server/Dockerfile
    command: sh -c "npm install && npm run start-dev"
    ports:
      - 127.0.0.1:3000:3000
    working_dir: /app/server
    volumes:
      - ./server:/app/server